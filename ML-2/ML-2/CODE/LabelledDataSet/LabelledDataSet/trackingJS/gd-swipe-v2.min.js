if (window.Swipe) {
    (function(a, g, c, f) {
        var e = "gdSwipe", b = {
            auto: 0,
            continuous: true,
            show_bottom_nav: true,
            stopPropagation: true,
            pauseOnHover: false
        };
        function d(h, i) {
            this.element = h;
            this.options = a.extend({}, b, i);
            this._defaults = b;
            this._name = e;
            this.init()
        }
        d.prototype.init = function() {
            var o, k, p, h, m, j = a(this.element).siblings(".gd-swipe-bottom-nav"), l = a(this.element).siblings(".gd-swipe-left-nav"), n = a(this.element).siblings(".gd-swipe-right-nav");
            this.options.callback = function(s) {
                var r = a("li", j);
                var q = r.length;
                while (q--) {
                    r[q].className = ""
                }
                r[s].className = "active"
            };
            o = Swipe(this.element, this.options);
            m = o.getNumSlides();
            if (this.options.pauseOnHover) {
                a(this.element).mouseenter(function(i) {
                    o.pause()
                });
                a(this.element).mouseleave(function(i) {
                    o.unpause()
                })
            }
            if (j.length) {
                p = c.createElement("ul");
                j.append(p);
                for (k = 0; k < m; k++) {
                    h = (k == 0) ? "active" : "";
                    a(p).append('<li class="' + h + '"><a href="#" data-slide="' + k + '">&bull;</a></li>')
                }
            }
            a("a[data-slide]", a(this.element).parents(".gd-nav-container")).click(function(i) {
                i.preventDefault();
                var q = a(this).data("slide");
                if (q === "prev") {
                    o.prev();
                    return true
                }
                if (q === "next") {
                    o.next();
                    return true
                }
                o.slide(q)
            })
        };
        a.fn[e] = function(h) {
            return this.each(function() {
                if (!a.data(this, "plugin_" + e)) {
                    a.data(this, "plugin_" + e, new d(this, a(this).data("gd-swipe") || h))
                }
            })
        };
        a("[data-gd-swipe]").gdSwipe()
    })(jQuery, window, document)
};
